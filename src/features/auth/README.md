# Auth Feature

Owner: Mongkol

Tickets:

- BCRS-2 Auth

Use this folder for protected route guards, role checks, and auth helpers. Shared auth state currently lives in `src/context/AuthContext.jsx`, and global routing currently lives in `src/App.jsx`.
